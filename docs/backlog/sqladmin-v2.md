# SQLAdmin v2 backlog (not scheduled)

Ideas parked after **SQLAdmin v1 freeze** (`cpaas-sqladmin-v1`). Implement only when there is a concrete ops need or a new domain model.

| Item | Notes |
|------|--------|
| Viewer / Operator / Admin RBAC | Roles already reserved in ADR-0022 |
| OAuth / OIDC instead of Basic | Stronger ops identity |
| Ops dashboard metrics | DB/Redis/queue chips beyond `/admin/home` |
| DeliveryAttempt view | After persistence model exists |
| WebhookEvent view | After persistence model exists |
| Advanced search / export | Beyond current SQLAdmin filters |

Do **not** start these in the same iteration as CPaaS partner onboarding.
